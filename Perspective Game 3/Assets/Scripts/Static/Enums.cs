using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum ViewType 
{ 
    normal, 
    vertical, 
    horizontal 
};

public enum Direction
{
    forward,
    backward,
    right,
    left,
    none
};